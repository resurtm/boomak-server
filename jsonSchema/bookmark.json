{
  "$schema": "http://json-schema.org/schema#",
  "description": "Validates bookmark submit data",
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "minLength": 10,
      "maxLength": 1000,
      "format": "url"
    }
  },
  "minProperties": 1,
  "maxProperties": 1,
  "required": [
    "url"
  ]
}
